terraform {
  backend "azurerm" {
    resource_group_name  = "sre-demo-rg"
    storage_account_name = "sretfstateacct123"
    container_name       = "tfstate"
    key                  = "terraform.tfstate"

    use_azuread_auth = true
    use_msi          = true
  }
}
